################################################################################
# PHP-FPM upstream
# 'php' resolves via Docker's embedded DNS on ac-249-network.
################################################################################
upstream fastcgi_backend {
    server php:9000;

    # Hold connections open between requests; avoids a TCP handshake per hit.
    keepalive 16;
}
